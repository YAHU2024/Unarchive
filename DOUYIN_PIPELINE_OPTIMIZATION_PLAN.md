# 抖音处理与同步性能修复计划

> 创建日期：2026-08-11
>
> 基线分支：`dev`
>
> 基线提交：`2f9ad30`、`0e86162`
>
> 状态：计划实施完成；本地与单目标真实验收完成；12 分钟性能门槛未通过，批量外部写入边界见 9.5

## 1. 背景与实测基线

2026-08-11 的真实页面测试确认，抖音收藏夹真实性修复已经生效：

- 账号下 45 个收藏夹能够完整枚举。
- 收藏夹 `7385866895024723727` 元数据为 14 条，接口实际返回 13 条可访问视频；程序保留 13 条并给出失效/变更提示。
- 收藏夹 `7365800256132667136` 返回 4/4 条，未再混入其他收藏夹预加载的数据。

同一轮 4 视频测试从开始抓取到完成分析约 20 分 23 秒。核心处理约 20 分 05 秒，其中：

| 阶段 | 观测耗时 | 占核心处理比例 |
|---|---:|---:|
| 媒体下载 | 约 170 秒 | 约 14% |
| CPU faster-whisper | 约 780 秒 | 约 65% |
| LLM 分析 | 约 255 秒 | 约 21% |

同步阶段还暴露出两个范围问题：

- 本轮只新处理 4 个视频，但同步输入为空时默认遍历全部 38 张本地历史卡片。
- ima 查重对每个视频执行两种标题查询，公共前缀命中较多时继续翻页，真实日志两次触发 2/4/8/16 秒频控退避。

## 2. 目标与非目标

### 2.1 目标

1. 空同步输入默认只处理本次成功或可恢复的卡片，不再隐式同步全部历史卡片。
2. 保持抖音真实收藏夹隔离和完整分页行为，不重新引入跨收藏夹混入。
3. 在同一 CPU、同一 4 视频样本上，将核心处理的第一阶段目标降到 12 分钟以内。
4. 下载失败时快速切换可靠路径，避免将完整媒体一次性读入内存。
5. 同一视频在重试、恢复和签名 URL 更新后能够复用已经下载的媒体。
6. 长逐字稿不再静默丢弃中间内容。
7. ima 优先使用本地目标隔离状态，减少远端搜索并主动控制请求节奏。
8. 日志保留可诊断信息，但不记录完整签名 URL、Cookie、密钥或不必要的内容标识。

### 2.2 非目标

- 不实现或维护抖音 X-Bogus 逆向签名。
- 不引入后台任务队列、数据库或多进程 Whisper。
- 不并行运行多个 CPU Whisper 实例。
- 不改变 Bilibili 平台字幕优先的既有语义。
- 不用自动化测试替代真实账号、真实页面和真实外部 API 验收。

## 3. 阶段一：同步范围与幂等性

### 3.1 实现

- `app.py`
  - 在处理完成后把本次卡片 ID 写入 Gradio state。
  - 飞书和 ima 的同步输入为空时，优先读取本次处理 ID。
  - 没有本次结果时停止并提示用户显式输入 ID，不再静默扫描全部本地知识卡片。
  - 对输入 ID 去空、稳定去重，并在调用外部 API 前记录目标数量。
- `src/sync/feishu.py`
  - 保持目标文件夹内按 `[video_id]` 前缀查重。
  - 对同名默认文件夹选择给出可诊断日志，避免创建行为不可见。
- `src/sync/ima.py`、`app.py`
  - 当本地状态已经证明当前 `(video_id, knowledge_base_id, folder_id)` 完成时，直接跳过远端搜索。
  - 当本地状态保存了 `note_id` 但知识库关联未完成时，直接恢复关联，不先执行标题搜索。
  - 状态缺失或不可信时才使用远端搜索兼容历史笔记。

### 3.2 测试

- 空输入只同步本批结果。
- 空输入且无本批结果时不发出外部请求。
- 显式 ID 列表保持顺序并去重。
- ima 当前目标已完成时零搜索请求。
- ima 部分成功状态直接恢复知识库关联。
- 知识库或文件夹切换后不能错误复用旧目标状态。

### 3.3 验收门槛

- 自动化测试通过。
- 4 视频批次的同步预览和实际调用数量均为 4。
- 同一目标第二轮同步不创建重复文档/笔记。

### 3.4 计划提交

`fix(sync): 默认仅同步本次处理结果`

`perf(ima): 复用本地状态减少远端查重`

## 4. 阶段二：抖音媒体下载与缓存

### 4.1 实现

- `src/scraper/douyin.py`
  - 从 `video.bit_rate` 中选择包含完整音轨的最低码率 H.264 播放地址。
  - `play_addr_h264`、`play_addr`、`download_addr` 继续作为结构缺失时的兼容回退。
  - 抖音 CDN 使用带登录 Cookie/Referer 的 httpx 流式下载作为主路径。
  - Playwright 浏览器上下文只作为短超时回退。
  - 下载先写 `.part` 文件，成功后原子替换，失败时清理不完整文件。
  - 异常日志只保留主机、路径和状态码，不记录查询参数。
- `src/transcript/subtitle.py`、`src/transcript/whisper_asr.py`
  - 缓存键改为 `platform + video_id`，不再依赖会变化的签名 URL。
  - 已存在且非空的媒体文件直接复用。
  - 正常流程结束仅释放模型；缓存使用期限和容量策略清理，不再全部删除。

### 4.2 测试

- 多个码率候选稳定选择最低 H.264 地址。
- 缺少码率列表时按兼容顺序回退。
- httpx 成功时不调用 Playwright。
- httpx 失败时调用 Playwright 回退。
- 流式下载失败不留下可被误用的完整缓存名。
- 同一视频签名 URL 变化后命中同一缓存。
- 缓存修剪只删除超过策略的文件。

### 4.3 验收门槛

- 真实抖音样本不再出现先等待 Playwright `ECONNRESET` 后才进入 httpx 的固定损耗。
- 日志中不出现完整签名 URL 查询串。
- 重跑同一视频能够复用媒体缓存。

### 4.4 计划提交

`perf(douyin): 使用低码率流式媒体下载`

`feat(transcript): 按视频复用媒体缓存`

## 5. 阶段三：Whisper 性能与流水线

### 5.1 实现

- `src/transcript/whisper_asr.py`
  - 将 `beam_size` 和 `language` 变为显式配置。
  - 快速模式使用 CPU int8、`beam_size=1` 和自动语言检测。
  - 质量模式保留 `beam_size=5`，允许明确指定语言。
  - 记录模型、设备、计算类型、beam、语言和实际转写耗时。
- `config.py`、`app.py`、`cli.py`
  - GUI 提供快速/质量模式选择；CLI 和环境配置具备相同语义。
  - 默认选择基于本轮固定样本实测决定，不在验证前宣称质量等价。
- `app.py`
  - 保持单个 Whisper 工作负载。
  - 在视频 N 的 LLM 网络调用期间，仅预取视频 N+1 的字幕/媒体输入。
  - 预取失败不影响当前卡片保存，并在下一轮按普通路径重试。
  - 取消、配额耗尽和异常退出时回收预取任务。

### 5.2 测试

- 快速/质量模式参数正确传入 faster-whisper。
- 自动语言模式不传递固定 `zh`。
- 任意时刻最多一个 Whisper 转写任务。
- LLM 阶段能够与下一视频预取重叠。
- 取消和异常路径不遗留后台任务或 `.part` 文件。

### 5.3 验收门槛

- 固定短、中、长样本分别记录快速/质量模式耗时。
- 同一 4 视频样本核心处理目标不超过 12 分钟；如硬件上未达标，保留实测数据并明确阻塞原因，不伪造通过。
- 对中英混合样本人工抽检关键术语和段落完整性。

### 5.4 计划提交

`perf(transcript): 增加 Whisper 快速处理模式`

`perf(pipeline): 在 LLM 阶段预取下一视频`

## 6. 阶段四：长文本完整性与 ima 节流

### 6.1 实现

- `src/analyzer/llm_analyzer.py`、`prompts/`
  - 超过单次上下文预算时按段落/字幕边界切块。
  - 每块生成事实摘要，再使用压缩结果执行现有结构分析和总结。
  - 保留原始逐字稿和分段记录，卡片中记录是否使用分块分析。
  - 分块失败时保存部分卡片，不能回退为静默头尾截断。
- `src/sync/ima.py`
  - 对确需执行的 `search_note` 加入单客户端最小请求间隔。
  - 频率限制继续指数退避，但同一批次不重复执行等价查询。
  - 日志区分主动节流、频率限制、每日额度和业务失败。

### 6.2 测试

- 长文本所有输入区间均被某个块覆盖。
- 块之间边界稳定且不会无限拆分。
- 短文本保持原来的两路并行分析。
- 分块结果能够进入最终结构分析。
- ima 等价查询只执行一次，连续搜索满足最小间隔。
- 每日额度错误立即停止，频率限制按上限重试。

### 6.3 验收门槛

- 14705 字符样本不再出现 `14705 -> 12000` 的内容截断日志。
- 最终卡片能够覆盖视频开头、中段和结尾的人工抽检要点。
- 4 视频当前批次同步不触发 ima 频率限制；38 卡压力测试如仍受平台限制，应以可恢复状态安全结束。

### 6.4 计划提交

`feat(analyzer): 分块处理长视频逐字稿`

`perf(ima): 节流并合并笔记查重请求`

## 7. 最终验证矩阵

### 7.1 自动化

```powershell
python -m pytest -q
python -m compileall -q app.py cli.py config.py src tests
python cli.py --help
git diff --check
```

### 7.2 本地集成

- 构建并关闭 Gradio UI。
- GUI/CLI 使用相同 Whisper 配置和缓存路径。
- 模拟首次处理、缓存恢复、LLM 失败恢复和第二轮跳过。
- 验证同步默认范围、本地状态目标隔离和中断恢复。

### 7.3 真实页面/API

- 抖音：加载 45 个收藏夹；复测 13/14 和 4/4 两个收藏夹。
- 处理：使用固定 4 视频样本记录下载、ASR、LLM 和总耗时。
- 飞书：本批 4 卡首次同步和同目标第二轮去重。
- ima：本批 4 卡首次同步、同目标第二轮去重、目标文件夹切换恢复。

真实验收依赖现有登录态、网络、LLM/飞书/ima 凭据和平台额度。无法执行时必须明确记录为“待用户复测”，不能标记为通过。

## 8. 完成定义

- 所有计划内代码和测试完成，阶段提交可独立回溯。
- 自动化、编译、CLI、Gradio 后端和差异检查通过。
- README 与 `PLAN.md` 只更新已经验证的事实。
- 真实平台验收有日志证据；无法由当前环境完成的项目列出精确操作和预期结果。
- 工作区不遗留本计划产生的未提交代码或临时文件。

## 9. 实施记录与当前验收结论

### 9.1 已提交阶段

| 阶段 | 提交 | 结果 |
|---|---|---|
| 遗留端口兼容 | `2f9ad30` | Gradio 端口被占用时自动寻找可用端口 |
| 遗留抖音收藏夹修复 | `0e86162` | 收藏夹分页隔离、详情缓存和重复标题定位 |
| 计划文件 | `cfe5db6` | 建立本计划和验收门槛 |
| 同步范围与本地状态 | `712cd37` | 空输入只同步本次处理结果；ima 状态优先恢复 |
| 媒体下载与缓存 | `c78dbe0` | 低码率 H.264、httpx 流式下载、稳定缓存和修剪 |
| Whisper 与媒体预取 | `1b88394` | 快速/质量预设、自动语言和下一媒体预取 |
| 长文本分块与查重缓存 | `183afd2` | 长稿覆盖式分块、事实摘要和 ima 查重缓存 |
| ima 节流 | `20768ae` | 标准前缀优先、历史格式回退、0.8 秒最小间隔 |
| ima 重复关联恢复 | `bf00300` | 将业务码 220001 作为当前知识库的幂等成功 |
| 飞书验收入口 | `88d4e44` | 修复真实验收脚本直接运行入口 |
| 抖音登录回退 | `b370501` | 有持久化 Cookie 时跳过无效 CDP 的 120 秒等待 |
| 分块状态记录 | `1b2acad` | 卡片写入 `analysis_chunked` 布尔标记 |
| 媒体日志隐私 | `c857b6b` | 抑制第三方完整请求 URL，增加临时媒体验收脚本 |
| 飞书批量验收 | `75151a4` | 验收脚本支持重复 `--video-id` 参数 |

### 9.2 本地自动化验收

2026-08-12 最终结果：

- `python -m pytest -q`：`169 passed`。
- `python -m compileall -q app.py cli.py config.py src tests`：通过。
- `python cli.py --help`：通过。
- `python cli.py process --help`：通过，包含 `--whisper-profile {fast,quality}`。
- `python -c "import app; ui=app.build_ui(); ui.close()"`：通过。
- `git diff --check`：通过。

### 9.3 真实抖音与内容完整性验收

- 登录态下完整枚举 45 个收藏夹。
- 收藏夹 `7365800256132667136` 返回固定样本 4/4，四条均为 `AVAILABLE`，没有混入其他收藏夹视频。
- 收藏夹 `7385866895024723727` 两页返回 13/14，并明确提示元数据与可访问数量不一致。
- `tests/douyin_media_acceptance.py` 对短样本通过 httpx 主路径下载 `22,128,948` 字节，耗时 `6.4s`；临时文件自动清理，日志不再出现签名查询串。
- 当前短样本实际从 `play_addr_h264` 获得完整音轨；`bit_rate` 最低 H.264 选择已有自动化覆盖，但该真实响应没有走到该分支。
- 长样本生成 177 个 Whisper 片段、16477 字符逐字稿，时间覆盖 `0.00s` 到 `1141.87s`；开头、中段和结尾抽检均为连贯英文内容。
- 长稿日志为 `16477 -> 1237`，明确记录“原文未截断”；后续卡片会写入 `analysis_chunked=true`。
- 媒体缓存共 4 个完整文件、约 94 MB，没有零字节文件或 `.part` 残留。

### 9.4 性能复测与结论

- 四视频冷启动在验收命令的 `1204.1s` 上限处被终止：前三张卡片已完成，第四条媒体已完整缓存但尚未完成转写，不能构造连续总耗时。
- 因运行到 20 分钟仍未完成，计划的“同一 4 视频不超过 12 分钟”门槛判定为未通过。
- 第四条 19:02 长视频使用缓存续跑总计 `770.5s`：其中无效 CDP 登录等待约 128 秒、Whisper `296.9s`、LLM 约 329 秒；LLM 内含一次请求失败和重试。
- 登录等待已在 `b370501` 修复。修复后的只读登录、45 收藏夹枚举、4/4 收藏夹读取和单视频探测合计 `17.6s`，日志确认不再等待 120 秒。
- 下载不再是主要瓶颈；当前限制是 CPU `small/int8/beam=1` 转写和长文本的多轮远端 LLM 延迟。为保留完整内容，没有通过改回头尾截断来满足数字。
- 与优化前相比，真实下载主路径已降到秒级，19:02 音频 Whisper 为 296.9 秒，登录固定损耗已消除；但端到端目标仍未达标，应按未通过处理。

### 9.5 同步验收与外部边界

- ima 单视频两轮真实验收首次发现 `220001`“知识重复添加”未收敛；`bf00300` 修复后同一视频得到 4 个 PASS、0 个失败，第二轮决策为 `skip`。
- 飞书单视频两轮真实验收通过：两轮均复用同一目标文件夹、识别已有文档并报告 1/1 成功。
- 四卡飞书批量写入命令已准备，但外部权限审查拒绝发送四张可能含私有逐字稿的卡片；需要用户明确批准这批数据和目标后运行。
- ima 当前知识库根目标的单卡幂等已通过；目标文件夹切换需要另一个明确的真实 folder ID，当前配置为空，不能擅自选择外部目标。
- 快速档在固定长样本上已有耗时和内容抽检；质量档的同硬件短/中/长对照没有追加执行，因为性能门槛已明确失败，不能据此宣称质量等价。

待明确授权后的命令：

```powershell
python tests/feishu_acceptance.py `
  --video-id 7445539589361634595 `
  --video-id 7429370120201456931 `
  --video-id 7415703448252861747 `
  --video-id 7356908570262228250
```

本计划的代码实施、自动化和可安全执行的真实验收已经完成。性能门槛、四卡外部写入和 ima 文件夹切换仍保持未通过/阻塞状态，不能标记为全绿。
