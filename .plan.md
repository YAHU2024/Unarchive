# 修复 LLM API 调用失败 BUG

## Context

抖音内容提取流程中，Whisper 转写成功后，LLM 分析步骤失败，错误信息为：
```
LLM 分析失败: LLM API 调用在 3 次重试后仍失败: 
```
冒号后面为空——说明 `last_error` 的 `str()` 为空字符串，无法定位真正的失败原因。

## 根因分析

`src/analyzer/llm_analyzer.py` 的 `_call_llm` 方法存在两个问题：

1. **httpx 客户端缺少 `trust_env=False`**：httpx 默认读取系统代理环境变量，可能导致请求走代理失败或超时。`main()` 虽在启动前清除了 `HTTP_PROXY/HTTPS_PROXY`，但 `LLMAnalyzer` 创建时可能仍有其他代理变量（如 `ALL_PROXY`）残留。

2. **错误信息丢失**：
   - `httpx.HTTPStatusError` 的 `__str__` 可能不包含响应体内容，当 API 返回错误（如 400/401/429）时，关键的错误原因（在响应 body 中）被丢弃
   - `KeyError` 的 `__str__` 对于空字符串 key 返回空串
   - 没有任何地方记录 API 实际返回了什么，导致完全无法诊断

## 修复方案

### Task 1: 修复 `src/analyzer/llm_analyzer.py`

**文件**: `src/analyzer/llm_analyzer.py`

修改点：

1. **httpx 客户端添加 `trust_env=False`**（第 45-51 行）：
   ```python
   self._client = httpx.AsyncClient(
       timeout=httpx.Timeout(connect=10.0, read=120.0, write=10.0, pool=10.0),
       trust_env=False,  # 禁用环境变量代理
       headers={...},
   )
   ```

2. **`_call_llm` 方法改进错误处理**（第 136-157 行）：
   - 在 `response.raise_for_status()` 之前，先检查响应状态码
   - 对于非 200 响应，读取并记录响应体内容到错误信息中
   - 对 `KeyError` 单独处理，记录实际收到的 response data
   - 确保最终 RuntimeError 包含有意义的错误信息

   ```python
   except httpx.HTTPStatusError as e:
       last_error = e
       # 读取响应体以获取有意义的错误信息
       try:
           body = e.response.text[:500]
           logger.warning("LLM API 返回 %d, body: %s", e.response.status_code, body)
       except Exception:
           pass
       logger.warning("LLM API 调用失败 (第 %d 次): %s %s", attempt, e.response.status_code, e)
   except KeyError as e:
       last_error = e
       logger.warning("LLM 响应格式异常 (第 %d 次), 缺少字段: %s, 响应: %s", 
                       attempt, e, str(data)[:500] if 'data' in dir() else "N/A")
   except httpx.RequestError as e:
       last_error = e
       logger.warning("LLM API 请求失败 (第 %d 次): %s", attempt, e)
   ```

   最终 raise 时拼接更清晰的错误信息：
   ```python
   error_detail = str(last_error) or repr(last_error)
   raise RuntimeError(f"LLM API 调用在 {self._max_retries} 次重试后仍失败: {error_detail}")
   ```

## 验证方式

1. 启动应用 `python app.py`
2. 登录抖音 → 获取收藏夹 → 处理视频
3. 观察 LLM 分析步骤：
   - 如果成功：应正常完成分析
   - 如果失败：日志中应能看到具体的 API 错误码和响应内容，而非空字符串
